export default function hookPlugin({ rmFiles, beforeBuild, afterBuild, }: {
    rmFiles?: string[];
    beforeBuild?: () => void;
    afterBuild?: () => void;
}): {
    name: string;
    buildStart(): void;
    buildEnd(err?: Error): void;
};
